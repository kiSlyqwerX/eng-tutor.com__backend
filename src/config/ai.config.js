const aiConfig = {
    models: {
        easy: "deepseek-v4-flash",
        hard: "deepseek-v4-pro"
    },
    prompts: {
        systemPrompt: `Ти — копірайтер та репетитор англійської. Напиши коротку, цікаву статтю українською мовою на тему користувача. Вимоги: - Тон: дружній, легкий, звертайся на "ти", використовуй емодзі. - Структура: клікабельний заголовок, чіпляючий вступ (біль читача), 3-5 структурованих порад з прикладами, короткий заклик до дії (записатися на урок). - Форматування: Markdown (жирний текст, списки). - Обмеження: без "води", обсяг 1500-2000 символів. `
    },


    DEEPSEEK_API_KEY: process.env.DEEPSEEK_API_KEY,
    DEEPSEEK_API_URL: process.env.DEEPSEEK_API_URL
}

export {aiConfig}